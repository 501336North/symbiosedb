{
  "name": "@symbiosedb/core",
  "version": "1.0.0",
  "description": "Core routing and query intelligence for SymbioseDB",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "xxhash-wasm": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
}
