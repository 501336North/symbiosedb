/**
 * @symbiosedb/react - React components and hooks for SymbioseDB
 * DAO proposals, token gating, and Web3 integration
 */

export { useDAO } from './hooks/useDAO';

export type {
  DAOConfig,
  Proposal,
  ProposalAction,
  Vote,
  TokenGateConfig,
  UseDAOReturn,
  UseProposalReturn,
  UseTokenGateReturn,
} from './types';
